language: go

sudo: required
dist: trusty

go:
  - "1.10.x"

addons:
  apt:
    packages:
      - flawfinder
      - squashfs-tools
      - uuid-dev
      - libuuid1
      - libssl-dev
      - libssl1.0.0
      - libarchive-dev

before_install:
  - 

install:
  - ./compile.sh --clean

script:
  - echo "Passed"
