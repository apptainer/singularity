#!/bin/bash -ex

# this script runs as root under docker 
# OS_VERSION is in $1

yum install -y libtool rpm-build make
./autogen.sh
./configure
make dist
rpmbuild -ta *.tar.gz
rpm -iv ~/rpmbuild/RPMS/*/*.rpm
sed -i 's,^prefix=.*,prefix=/usr,' test.sh
useradd testuser
su testuser -c "make test"
