# oci test functions

check-container-state() {
    local state=$2
    local containerID=$1
    sleep 0.2
    status=$(sudo singularity oci state ${containerID} | execute-template "{{.status}}")
    test-match "OciState-${state}" "${state}" "${status}"
}