SUBDIRS = deps

modsdir = $(libexecdir)/singularity/mods

dist_mods_SCRIPTS = sappdir_filecheck install_file sappdir_delete sappfile_install  \
	build.sh install.sh

MAINTAINERCLEANFILES = Makefile.in

