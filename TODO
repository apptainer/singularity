2.3:
    ? Start a process daemon automatically when running a command and
      keep it running until all other process are done
    * create a src/util/user.c and move user info from privilege.c to
      there.
    * Evaluate changing the privileged open options to just trust
      containers that have +x
