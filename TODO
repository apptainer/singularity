

* Share loop device on same image:user
* Lock for loop device obtaining
* Create "singularity config file.img" for first run configurations (or similar)
* Create "singularity install file.img /path/to/recipe"
* Add relevent runtime options to all commands
