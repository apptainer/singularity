Features:
    * Add option to bind cwd if not already bound
    * Support tgz image format
    * --with-userns configure flag needs to be changed to enable/disable
        suid compoents
    * Add support for scratch directory

Fixes:
    * For older distros that do not support loop autoclear, conditionally
        create a child process and move mount namespace to a child process,
        and have parent umount when done
    * Go over singularity.conf and finish adding features back in

New API porting:
    * signal handlers
    * tmpfs overlay
    * scratch directory creation
    * make loop device multi-process compatible
    * clean old sources
