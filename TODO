* Move tmp bind mounts to separate module, so when -C is passed it can use sessiondir
* Add option to bind cwd if not already bound
* Only mount proc if PID namespace unshared (otherwise bind)
    * Do we bind proc and sys?
